
<div class="container">
    <h1 data-test-title>Posts</h1>
    <div id="postsContainer">
     {{outlet}}
        {{#each model as |post|}}
          {{log post.title}}
          <h1>{{post.title}}</h1>
        {{/each}}
    </div>
</div>